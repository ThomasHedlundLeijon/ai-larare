[build]
  publish = "."
  command = ""

[functions]
  directory = "netlify/functions"
  node_bundler = "nft"

[functions."claude"]
  timeout = 30

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
